1. add below dependecy in all ms
  <dependency>
            <groupId>io.opentelemetry.javaagent</groupId>
            <artifactId>opentelemetry-javaagent</artifactId>
            <version>2.11.0</version>
            <scope>runtime</scope>
        </dependency>

2. add below property in all ms

logging:
  pattern:
    level: "%5p [${spring.application.name},%X{trace_id},%X{span_id}]"

3. Added Tempo config file then its service and image details in compose file

4. updated data.yml in grafana to add Tempo as datasource

5.update common config file for env variables

6.Added Otel service names for env in compose file
